#include <stdio.h> 

int main() {

    int radius = 3;
    float pi = 3.14;
    // printf("THE AREA OF THE CIRCLE IS %f", pi*radius*radius);
    int height = 3;
    printf("VOLUME OF THE CYLINDER IS %f", pi * radius * radius * height);
return 0;
}