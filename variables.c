#include <stdio.h>

int main() {
    int a = 4;
    int k = 8;
    float b = 8.5;
    char c = 'u';
    // printf("The value of a is %d %c \n", a, c);
    // printf("The value of a is %d %c \n", a, c);
    // printf("The value of a is %d %f \n", a, b);
    printf("SUM is = %d", k+a );
    return 0;
}       